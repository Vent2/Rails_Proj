<h1>Welcome Back</h1>
<%= form_with(model: @user, local: true) do |form| %>

<%= render 'form', user: @user %>
<div class="actions">
    <%= form.submit :Login %>
  </div>
<%= link_to 'Back', root_path %>

<% end %>
